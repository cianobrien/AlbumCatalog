<p id="notice"><%= notice %></p>

<h1><%= @album.name %></h1>

<h2><%= @album.artist %>, <%= Album.find(params[:album_id]) => <%= @album.year %></h2>


<table>

  <td>
    <%= image_tag(@album.image_url, size: "400x400", class: 'list_image') %>
  </td>

<td class='list_tracks'>
	  <dl>
	    <pre>
              <%= button_to 'Add to Profile', 
		line_items_path(@album_id) %>
	      <h3>Tracks:</h3>
	      <%= @album.tracks %>
	    </pre>
	  </dl>
	</td>
</table>
